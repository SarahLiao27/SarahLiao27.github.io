import React from "react";
import VideoCarousel from "../components/VideoCarousel";

const HomePage = () => {
    return (
        <div>
            <VideoCarousel/>
        </div> 
    );
};

export default HomePage;